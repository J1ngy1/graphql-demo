type Post {
  id: ID!
  title: String
  body: String
  tags: [String]
  reactions: Reactions
  views: Int
  userId: Int
}

type Reactions {
  likes: Int
  dislikes: Int
}

type Query {
  posts: [Post]
  post(id: ID!): Post
}
